import { initializeApp } from "firebase/app"; import { getDatabase, ref, set } from "firebase/database"; const app = initializeApp({ databaseURL: "https://krux-ee1df-default-rtdb.firebaseio.com" }); const db = getDatabase(app); async function run() { console.log("Trying..."); try { await set(ref(db, "active_sessions/KRUX_BIN_001"), { status: "connected", user_id: "test", timestamp: Date.now() }); console.log("SUCCESS!"); } catch (err) { console.error("ERROR:", err.message); } process.exit(); } run();
